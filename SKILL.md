# SKILL.md —— VelaTime 自建 Skill 合集

本文件是 VelaTime 项目沉淀的两个**自定义 Skill** 的规范定义。

> 大赛要求"至少 1 个自定义 Skill"。我们做了两个：
> **① 产品 Skill**（学生任务管家）—— 给端侧 AI Agent 用，改变它的行为
> **② 开发流程 Skill**（真机迭代闭环）—— 给我们自己用，把反复踩的坑固化下来
>
> 每个 Skill 都按统一结构写：**触发词 → 操作步骤 → 输出规范 → 禁止事项**。

---

# Skill ①：学生任务管家（Student Task Planner）

> **这份 Skill 是产品的一部分** —— 设备端 AI Agent 加载它之后，
> 才能按 VelaTime 能解析的格式读写任务。

## 1.1 触发词（When to use）

| 意图 | 触发词 / 句式 |
|---|---|
| **创建** | 创建任务、记一下、帮我记、添加待办、提醒我、别忘了 |
| **查询** | 查看任务、我还有什么作业、这周要交什么、清单 |
| **推荐** | 现在做什么、接下来干什么、推荐一个任务、有空了 |
| **完成** | 完成任务、做完了、打勾、标记完成 |
| **延后** | 延后、晚点做、推迟、先放着 |
| **课程** | 今天有什么课、几点上课、教室在哪、什么时候空 |

**不触发**：纯闲聊、问天气、单纯问时间（后者直接调 `get_current_time`）。

## 1.2 操作步骤（What to do）

**创建任务**
```
1. 调 get_current_time 取当前日期（不要猜）
2. 把"今天/明天/后天/下周一"解析成绝对日期 YYYY-MM-DD
3. 检查必需字段（标题、截止日期）；缺哪个问哪个，一次只问一个
4. read_file /data/ai_agent/TASKS.md
     不存在 → write_file 创建
     已存在 → 保留全部原有内容，只追加新行
5. 追加：- [ ] [YYYY-MM-DD] 任务标题
6. 一句话回复：标题 + 截止日期
```

**查询任务**
```
1. read_file /data/ai_agent/TASKS.md
2. [ ] 视为待办，[x] 视为已完成；默认只列待办
3. 按截止日期升序
```

**完成任务**
```
1. read_file 任务文件
2. 定位目标行
3. edit_file 把 "- [ ]" 改成 "- [x]"
4. 找不到就不改文件，直接告知
```

**推荐任务**（主动提醒同用）
```
1. 取当前时间与星期
2. 读未完成任务
3. 读当天课程表，算出空闲窗口
4. 按「紧迫度 + 时长匹配」打分选最高
5. 给一句话理由，必须含依据（截止时间 / 空档长度）
```

## 1.3 输出规范（Output contract）

**任务文件格式 —— 硬约束，VelaTime 依赖它解析：**

```markdown
- [ ] [2026-09-20] 交高数作业
- [ ] [2026-09-22] 写物理实验报告
- [x] [2026-09-18] 预习讲义
```

| 规则 | 说明 |
|---|---|
| 一行一个任务 | 不换行、不用多行描述 |
| 固定前缀 | `- [ ]` 待办 / `- [x]` 已完成 |
| 日期在方括号内 | `[YYYY-MM-DD]`，**是截止日期，不是创建日期** |
| **不加别的字段** | 优先级、标签、负责人都不要 —— 界面解析不了 |

**为什么这么严**：VelaTime 每秒轮询这个文件并正则解析，格式一乱手表就不显示。

**对话回复格式**

| 场景 | 示例 |
|---|---|
| 创建成功 | `已记下：交高数作业，9月20日截止` |
| 查询 | `还有 3 件：1. 交高数作业（9月20日）…` |
| 推荐 | `建议现在写高数作业 —— 明天 17:00 截止，你有 120 分钟空档` |
| 完成 | `已完成：交高数作业。还剩 2 件` |
| 信息不足 | `这份作业哪天截止？` |

**回复一律简短**（屏幕 454×454，长文本显示不下）。

## 1.4 禁止事项

| 禁止 | 原因 |
|---|---|
| ❌ 猜测/编造截止日期 | 会误导用户 |
| ❌ 删除已有任务行 | 用户没要求时绝不动 |
| ❌ 把截止日期说成创建日期 | 语义错误 |
| ❌ 不先读就重写整个文件 | 会丢已有任务 |
| ❌ 一次问多个缺失字段 | 小屏交互，一次一个问题 |
| ❌ 长篇回复 | 454 圆屏放不下 |

## 1.5 部署位置

```
源文件      app/velatime/skills/student-task-planner.md
运行时副本  core/core_agent_sync.c 内嵌 g_student_task_skill[]
            → 启动时写入设备 /data/ai_agent/skills/task-manager.md
消费方      ui/ui_tasks.c（任务列表/详情）+ 每秒轮询 TASKS.md
```

**为什么内嵌**：板上 `/data` 是 tmpfs，没法预置文件，只能编译期内嵌。

---

# Skill ②：真机迭代闭环（On-device Iteration Loop）

> **这份 Skill 给我们自己用** —— 把这次开发中反复踩的坑固化下来。
> 下次改代码到真机验证，按这个流程走就不会重踩。

## 2.1 触发词（When to use）

- "改一下真机上的 X"、"不生效"、"烧进去没变化"
- "编译好了但镜像 MD5 没变"
- "板子跑不起来 / 卡住 / 复位了"
- "串口没有输出"

## 2.2 操作步骤（What to do）

**A. 改代码 → 编译**

```bash
# 只想改 .c 文件
./build.sh vendor/bes/boards/best1700_ep/aos_evb/configs/ap --cmake -j4

# ⚠️ 改了 rcS.ap / 板级 CMakeLists / 加新源文件 → 必须强制重配
touch vendor/bes/boards/best1700_ep/aos_evb/src/CMakeLists.txt
rm -f cmake_out/aos_evb_ap/etc/init.d/rcS \
      cmake_out/aos_evb_ap/romfs_etc/init.d/rcS \
      cmake_out/aos_evb_ap/romfs.img
```

**怎么判断有没有真的重配**：ninja 目标数只有 ~18 就是没重配，2000+ 才对。

**B. 烧录**

```bash
dldtool.exe 3 --reboot .\programmer1700_dual.bin --set-dual-chip 1 \
  -M .\nuttx_ap.bin --pgm-rate 2000000
```

- 成功标志：`PROGRAMMING SUCCEEDED`
- 失败标志：`FLASH_CMD_BURN_DATA 4 error=0x69`
- **dldtool 会等最多 ~76 秒同步，期间任意时刻按 RESET 都能进**

**C. 验证（三层，缺一层都可能误判）**

```
① 镜像层：MD5 变了没有？关键字符串在不在？
     python3 -c "d=open('nuttx_ap.bin','rb').read(); print(d.count(b'你的新字符串'))"

② 系统层：串口 ps / free / ifconfig / date
     确认进程在跑、内存够、网络通、时钟对

③ 行为层：实际操作一遍，看串口日志
     例：滑动切页 → 日志应出现 swipe left/right
```

**D. 出问题时的排查顺序**

```
板子没反应        → 先看串口有没有输出（不输出=可能没烧进去或没起）
烧了没变化        → 查 MD5 是否真的变了；查是不是有别的进程占着显示（ps）
改动不生效        → rcS/CMake 类的改动要强制重配（见 A）
卡住不动          → 抓串口日志看卡在哪一步，注意有没有看门狗复位
```

## 2.3 输出规范（Output contract）

**每次真机验证必须留下这三样，写进 `logs/`：**

| 产物 | 格式要求 |
|---|---|
| **镜像指纹** | MD5 + 文件大小 + 编译时间 |
| **串口日志** | 关键片段原文（不要只写"成功了"），带时间戳 |
| **命令与预期** | 成对出现：`命令` → `期望输出` |

**示例**：
```
$ date
Sat, Sep 19 15:40:17 2026          ← 系统时钟（UTC）

$ time_sync
time_sync: www.baidu.com -> 1789832424
time_sync: clock set to 2026-09-19 23:40:24 CST (UTC+8)
```

**禁止**：只写结论不贴证据（"已验证通过"这种不算验证）。

## 2.4 禁止事项

| 禁止 | 原因 |
|---|---|
| ❌ 只看编译成功就认为功能好了 | 编译成功 ≠ 跑得起来 |
| ❌ 烧录前不备份当前可用镜像 | 出问题回不去 |
| ❌ 改 rcS 后不强制重配 CMake | 镜像 MD5 不变，白烧 |
| ❌ 把 API Key / WiFi 密码写进要提交的文件 | 会泄漏 |
| ❌ 只凭模拟器结果就宣布真机可用 | 触摸等驱动相关部分差异很大 |

---

# 如何新增一个 Skill

1. 在 `app/velatime/skills/` 下新建 `.md`，按上面的四段结构写
2. 如果要让**设备端 Agent** 用：把内容内嵌到 `core/core_agent_sync.c`
   的 `g_student_task_skill[]` 形式，启动时写入 `/data/ai_agent/skills/`
3. 如果要让**我们自己**用：直接放进 `skills/`，在本文档登记
4. 在本文档末尾的登记表加一行

## Skill 登记表

| # | Skill | 类型 | 触发场景 | 定义位置 |
|---|---|---|---|---|
| ① | 学生任务管家 | 产品（Agent 用） | 创建/查询/完成/推荐任务 | `skills/student-task-planner.md` + 本文件 §1 |
| ② | 真机迭代闭环 | 开发（人用） | 改代码到真机验证 | 本文件 §2 |
